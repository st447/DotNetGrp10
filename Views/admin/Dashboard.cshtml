@model Health_Care_MIS.Models.AdminDashboardViewModel

@{
    ViewBag.Title = "Admin Dashboard";
}

<div class="content-section">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-primary mb-0">
            <i class="fas fa-tachometer-alt me-2"></i>
            Admin Dashboard
        </h2>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row g-4">-->
        <!-- Quick Stats -->
        <div class="col-12">
            <div class="row g-4">
                <div class="col-md-3">
                    <div class="card bg-primary bg-opacity-10 h-100">
                        <div class="card-body">
                            <div class="d-flex align-items-center">
                                <div class="avatar-lg bg-primary bg-opacity-10 rounded-circle me-3 d-flex align-items-center justify-content-center">
                                    <i class="fas fa-users text-primary fs-2"></i>
                                </div>
                                <div>
                                    <h6 class="text-muted mb-1">Total Patients</h6>
                                    <h4 class="mb-0">@Model.TotalPatients</h4>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card bg-success bg-opacity-10 h-100">
                        <div class="card-body">
                            <div class="d-flex align-items-center">
                                <div class="avatar-lg bg-success bg-opacity-10 rounded-circle me-3 d-flex align-items-center justify-content-center">
                                    <i class="fas fa-calendar-check text-success fs-2"></i>
                                </div>
                                <div>
                                    <h6 class="text-muted mb-1">Today's Appointments</h6>
                                    <h4 class="mb-0">@Model.TodayAppointments</h4>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card bg-info bg-opacity-10 h-100">
                        <div class="card-body">
                            <div class="d-flex align-items-center">
                                <div class="avatar-lg bg-info bg-opacity-10 rounded-circle me-3 d-flex align-items-center justify-content-center">
                                    <i class="fas fa-user-md text-info fs-2"></i>
                                </div>
                                <div>
                                    <h6 class="text-muted mb-1">Active Doctors</h6>
                                    <h4 class="mb-0">@Model.ActiveDoctors</h4>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card bg-warning bg-opacity-10 h-100">
                        <div class="card-body">
                            <div class="d-flex align-items-center">
                                <div class="avatar-lg bg-warning bg-opacity-10 rounded-circle me-3 d-flex align-items-center justify-content-center">
                                    <i class="fas fa-flask text-warning fs-2"></i>
                                </div>
                                <div>
                                    <h6 class="text-muted mb-1">Pending Lab Results</h6>
                                    <h4 class="mb-0">@Model.PendingLabResults</h4>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quick Links -->
        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="card-title">
                        <i class="fas fa-link me-2"></i>
                        Quick Links
                    </h5>
                </div>
                <div class="card-body p-0">
                    <div class="list-group list-group-flush">
                        <a href="@Url.Action("Index", "Registrations")" class="list-group-item list-group-item-action d-flex align-items-center">
                            <i class="fas fa-user-plus me-3 text-primary"></i>
                            Manage Registrations
                        </a>
                        <a href="@Url.Action("Index", "AppointMents")" class="list-group-item list-group-item-action d-flex align-items-center">
                            <i class="fas fa-calendar-alt me-3 text-success"></i>
                            View Appointments
                        </a>
                        <a href="@Url.Action("Index", "Prescriptions")" class="list-group-item list-group-item-action d-flex align-items-center">
                            <i class="fas fa-prescription me-3 text-info"></i>
                            View Prescriptions
                        </a>
                        <a href="@Url.Action("Index", "Bills")" class="list-group-item list-group-item-action d-flex align-items-center">
                            <i class="fas fa-file-invoice-dollar me-3 text-warning"></i>
                            View Bills
                        </a>
                        <a href="@Url.Action("Index", "Laboratories")" class="list-group-item list-group-item-action d-flex align-items-center">
                            <i class="fas fa-vial me-3 text-danger"></i>
                            View Lab Results
                        </a>
                        <a href="@Url.Action("Index", "Staffs")" class="list-group-item list-group-item-action d-flex align-items-center">
                            <i class="fas fa-user-md me-3"></i>
                            Manage Staff
                        </a>
                        <a href="@Url.Action("Index", "StaffSchedules")" class="list-group-item list-group-item-action d-flex align-items-center">
                            <i class="fas fa-calendar-alt me-3"></i>
                            Staff Schedules
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- User Management -->
        <div class="col-md-8">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="card-title">
                        <i class="fas fa-users-cog me-2"></i>
                        User Management
                    </h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="bg-light">
                                <tr>
                                    <th>
                                        <i class="fas fa-envelope me-2"></i>
                                        Email
                                    </th>
                                    <th>
                                        <i class="fas fa-user-tag me-2"></i>
                                        Current Role
                                    </th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var user in Model.Users)
                                {
                                    <tr>
                                        <td>
                                            <div class="d-flex align-items-center">
                                                <div class="avatar-sm bg-primary-subtle rounded-circle me-2 d-flex align-items-center justify-content-center">
                                                    <i class="fas fa-user text-primary"></i>
                                                </div>
                                                @user.email
                                            </div>
                                        </td>
                                        <td>
                                            <span class="badge bg-primary">@user.Role</span>
                                        </td>
                                        <td>
                                            @using (Html.BeginForm("UpdateRole", "Admin", FormMethod.Post))
                                            {
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="userId" value="@user.useId" />
                                                <div class="input-group">
                                                    <span class="input-group-text">
                                                        <i class="fas fa-user-shield"></i>
                                                    </span>
                                                    <select name="newRole" class="form-select">
                                                        <option value="admin" @(user.Role == "admin" ? "selected" : "")>Admin</option>
                                                        <option value="user" @(user.Role == "user" ? "selected" : "")>User</option>
                                                        <option value="doctor" @(user.Role == "doctor" ? "selected" : "")>Doctor</option>
                                                        <option value="finance" @(user.Role == "finance" ? "selected" : "")>Finance</option>
                                                    </select>
                                                    <button type="submit" class="btn btn-primary">
                                                        <i class="fas fa-save me-2"></i>
                                                        Update
                                                    </button>
                                                </div>
                                            }
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .content-section {
            animation: fadeIn 0.3s ease-out;
            padding: 1.5rem;
        }

        .avatar-lg {
            width: 64px;
            height: 64px;
        }

        .avatar-sm {
            width: 32px;
            height: 32px;
            font-size: 0.875rem;
        }

        .card {
            transition: transform 0.2s ease-in-out;
            border: none;
            box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075);
        }

        .card:hover {
            transform: translateY(-2px);
        }

        .list-group-item {
            border-left: none;
            border-right: none;
            padding: 1rem 1.5rem;
        }

        .list-group-item:first-child {
            border-top: none;
        }

        .list-group-item:last-child {
            border-bottom: none;
        }

        .table th {
            font-weight: 600;
            color: #495057;
        }

        .badge {
            font-weight: 500;
            padding: 0.5em 0.75em;
        }

        .input-group-text {
            background-color: #f8f9fa;
            color: #6c757d;
        }

        .alert {
            border-left-width: 4px;
        }

        .card-header {
            background-color: var(--bs-light);
            border-bottom: 1px solid rgba(0,0,0,.125);
        }

        .card-title {
            color: var(--bs-primary);
            font-size: 1.1rem;
        }
    </style>
}

@section Scripts {
    <script>
        $(document).ready(function () {
            // Enable Bootstrap tooltips
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
            var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                return new bootstrap.Tooltip(tooltipTriggerEl)
            });

            // Auto-hide success message after 3 seconds
            setTimeout(function () {
                $(".alert").fadeOut("slow");
            }, 3000);
        });
    </script>
}-->
